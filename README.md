# Student Project Suggestion Service

A stateless FastAPI service that generates project ideas using the Groq LLM API.

## Setup
```bash
pip install -r requirements.txt
```

## Run
```bash
uvicorn main:app --reload --env-file .env
```

## Environment Variables
Create a `.env` file:
